kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: node:24
    commands:
      - npm install
      - npm run build

  - name: deploy
    image: node:24
    commands:
      - npm install -g serve
      - serve -s build -l 3005 &
